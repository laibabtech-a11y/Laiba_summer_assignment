#include <stdio.h>

int main() {
    int number;


printf("enter an integer: ");
scanf("%d", &number);

printf("\nmultiplication table for %d:", number);
for (int i = 1; i <= 10; i++) {
    printf("\n%d * %d = %d", number, i, number * i);
}

return 0;
}